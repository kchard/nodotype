var create = function(data) {		data.sort(function(a, b) {		return a < b ? -1 : 1;	});		var getAll = function(success, error) {		success(data);	};		var get = function(id, success, error) {		var item;		for(var i = 0; i < data.length; i++) {			if(data[i].id == id) {				item = data[i];				break;			}		}		if(item) {			success(item);		} else {			error();		}	};	var create = function(item, success, error) {		data.sort(function(a, b) {			return a.id - b.id;		});				if(data.length === 0) {			item.id = 1;		} else {			item.id = data[data.length - 1].id + 1;		}				data.push(item);				success(item);	};		var update = function(item, success, error) {		for(var i = 0; i < data.length; i++) {			if(data[i].id === item.id) {				data[i] = item;				break;			}		}				success(item);	};		var destroy = function(id, success, error) {		for(var i = 0; i < data.length; i++) {			if(data[i].id == id) {				data.splice(i, 1);				break;			}		}				success();	};		return {get: get, getAll: getAll, create: create, update: update, destroy: destroy};};exports.create = create;